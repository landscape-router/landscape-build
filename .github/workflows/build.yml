name: Build x86 Image

on:
  workflow_dispatch:
    inputs:
      board:
        description: 'Board to build (e.g., uefi-x86, mangopi-m28k)'
        required: true
        default: 'uefi-x86'
  push:
    branches:
      - main
      - ci
    paths:
      - 'userpatches/**'
      - 'build.sh'
      - 'build.env'
      - '.github/workflows/**'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Maximize build space
        run: |
          echo "Before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /usr/local/share/powershell
          sudo rm -rf /usr/local/lib/node_modules
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          sudo apt-get clean
          # Removing large packages (ignore errors if package not found)
          sudo apt-get purge -y azure-cli google-cloud-sdk google-chrome-stable firefox powershell mono-devel || true
          sudo apt-get autoremove -y
          # Wipe Docker images and data
          sudo docker image prune -a -f
          sudo docker system prune -a -f --volumes
          echo "After cleanup:"
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-utils rsync curl git xz-utils

      - name: Build Armbian Image
        env:
          COLUMNS: 160
        run: |
          # Use the input board or default to uefi-x86
          SELECTED_BOARD="${{ github.event.inputs.board || 'uefi-x86' }}"
          echo "Building for board: $SELECTED_BOARD"
          
          # Create build dir on the larger /mnt disk and symlink to it
          # This provides ~60GB+ additional space
          sudo mkdir -p /mnt/armbian-build
          sudo chown runner:runner /mnt/armbian-build
          ln -s /mnt/armbian-build armbian
          
          sudo -E ./build.sh "$SELECTED_BOARD"

      - name: Prepare Artifacts
        run: |
          mkdir -p final_output
          echo "Compressing images (this may take a while)..."
          
          # The images are actually in the location where armbian/ was symlinked
          # Based on our build step, that is /mnt/armbian-build/output/images/
          IMAGE_DIR="/mnt/armbian-build/output/images"
          
          if [ -d "$IMAGE_DIR" ]; then
            cd "$IMAGE_DIR"
            for f in *.img *.vmdk; do
              if [ -f "$f" ]; then
                echo "Compressing $f..."
                xz -v -T0 -9 "$f"
              fi
            done
            cd -
            # Move compressed files to final_output in the current workspace
            mv "$IMAGE_DIR"/*.xz final_output/ 2>/dev/null || true
          else
            echo "Error: Directory $IMAGE_DIR does not exist. Check if build failed or directory structure changed."
            ls -R /mnt/armbian-build/ || true
            exit 1
          fi
          
          echo "Contents of final_output:"
          ls -lh final_output/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: landscape-image-${{ github.event.inputs.board || 'uefi-x86' }}
          path: final_output/
